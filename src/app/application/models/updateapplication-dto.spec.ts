import { UpdateapplicationDto } from './updateapplication-dto';

describe('UpdateapplicationDto', () => {
  it('should create an instance', () => {
    expect(new UpdateapplicationDto()).toBeTruthy();
  });
});
